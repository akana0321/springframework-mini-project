<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- dao의 interface와 연결하기 위해 namespace에 해당 인터페이스 경로를 줌 -->
<mapper namespace="com.mycompany.webapp.dao.CommentDao">
	<select id="selectByQno" parameterType="int" resultType="comment">
		SELECT comment_no, user_id, question_no, comment_content, comment_date
		FROM comments
		WHERE question_no=#{qno}
	</select>
	
	<select id="selectByCno" parameterType="int" resultType="comment">
		SELECT comment_no, user_id, question_no, comment_content, comment_date
		FROM comments
		WHERE comment_no=#{cno}
	</select>
	
	<select id="count" resultType="int">
		SELECT count(*) FROM comments
	</select>

	<!-- cno 시퀀스 처리 -->
	<insert id="insert" parameterType="comment">
		INSERT INTO comments 
		(comment_no, user_id, question_no, comment_content, comment_date) 
		VALUES(seq_comment_cno.nextval, #{uid}, #{qno}, #{ccontent}, sysdate)
	</insert>
	
	<delete id="deleteByCno" parameterType="int">
		DELETE FROM comments WHERE comment_cno = #{cno}
	</delete>
	
	<update id="update" parameterType="comment">
		UPDATE comments SET
				comment_content=#{ccontent}
		WHERE comment_no = #{cno}	
	</update>
</mapper>

